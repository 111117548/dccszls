# 贡献说明

感谢关注本项目。当前仓库以产品展示和内部协作为主，提交代码前请先通过Issue说明目标与影响范围。

## 开发要求

1. 使用微信开发者工具导入仓库根目录。
2. 不提交生产密钥、真实项目数据或个人信息。
3. 保持13类部件稳定ID，避免直接按显示文字关联历史记录。
4. 新增页面需兼顾真机安全区、触控尺寸和小程序WXSS兼容性。
5. 修改数字孪生时同时验证低/中画质、部件筛选和360°相机轨道。

## 提交前检查

```bash
node --test tests/*.test.js
node tests/static-check.js
node tests/runtime-smoke.js
```

Pull Request需说明业务背景、主要改动、测试结果，以及涉及界面时的真机截图。
