# 飞书多维表格整改闭环联调

目标表格已预置为：

- `app_token`: `UHjvbqHtrak96Usb1ItcwI8pnNe`
- `table_id`: `tbl8MwNtgzsHjG0A`
- 视图 ID 仅用于人工查看；接口不依赖视图 ID。

## 已实现的流程

1. 飞书同事在多维表格中新建整改项。
2. 小程序首页点击项目选择器，直接读取飞书表中已经存在的项目名称。
3. 选择飞书项目后，再选择该项目已有的炉号。系统只导入“项目名称 + 炉号”同时精确一致的整改项。
4. 导入任务仍可沿用现有「分享整改任务」能力；分享接收人无需绑定身份即可提交照片与文字。
5. 小程序项目方复验通过后，整改后的云存储照片会自动上传到飞书原记录的 `闭环` 附件字段。
6. 最终 `整改状态` 由飞书端相关部门维护；小程序下一次同步后显示该状态。

小程序不会自行把飞书 `整改状态` 改成“已整改”，避免覆盖其他部门的正式验收结论。

## 项目经理权限模型

飞书是项目权限的唯一来源，小程序不再自行分配项目成员：

- 项目经理首次使用时，优先通过微信原生手机号授权，将微信 `OPENID` 与同手机号的飞书员工身份绑定；手机号无法匹配时再使用飞书 OAuth 备用授权。
- 所有已绑定项目经理都能查看全部项目。
- 只有飞书 `项目负责人` 人员字段包含当前飞书用户的项目才可编辑。
- `FEISHU_PLATFORM_ADMIN_IDS` 中配置的平台管理员可以编辑全部项目。
- 整改人员仍使用随机分享令牌，只能编辑单条整改任务的说明和照片。

不得把项目经理姓名作为权限依据；权限核验使用飞书 `open_id/user_id/union_id`。即使前端按钮被篡改，`quality-ledger` 与 `feishu-rectification` 云函数仍会在写入前重新核验。

### 身份绑定配置（手机号优先，OAuth 备用）

正式用户默认使用“微信手机号一键绑定”：

1. 小程序必须完成企业主体认证，并在微信公众平台按要求开通手机号快速验证能力、补充用户隐私保护指引。
2. 飞书自建应用的应用身份权限中开通并发布“通过手机号或邮箱获取用户 ID”；如需显示真实姓名和头像，再开通读取用户基本信息权限。
3. 飞书应用通讯录可用范围必须包含需要使用小程序的员工。
4. 用户微信绑定手机号必须与公司飞书通讯录登记手机号一致。云函数仅保存手机号哈希和末四位，不保存明文手机号。
5. `feishu-auth` 根据手机号取得飞书 `open_id` 后建立一对一绑定；`quality-ledger` 与 `feishu-rectification` 每次写入仍根据可信的飞书人员字段核验项目编辑权限。

手机号绑定不产生 `user_access_token`。在此模式下，`feishu-rectification` 使用应用身份访问表格，但只有项目负责人或平台管理员通过服务端核验后才允许更新。应用必须已加入目标多维表格，并具备记录更新和附件上传权限。

飞书 OAuth 仅作为手机号不同或通讯录无法匹配时的备用方式：

1. 在飞书开放平台为当前自建应用启用网页应用/OAuth 能力。
2. 为 `cloudfunctions/feishu-auth` 配置云函数 HTTP 访问服务，把生成的 HTTPS 回调地址填写到飞书安全设置中的重定向 URL。
3. 将同一个地址配置为 `FEISHU_OAUTH_REDIRECT_URI`，必须逐字符一致。
4. 小程序不再使用 `<web-view>` 直接打开 `accounts.feishu.cn`，避免普通用户被微信业务域名校验拦截。每位用户点击绑定后都会生成与本人微信 `OPENID` 关联的 10 分钟专属链接；复制到系统浏览器或飞书中完成授权，再返回小程序自动确认。授权链接不可转发给其他人。

`feishu-auth` 环境变量：

| Key | 值 |
| --- | --- |
| `FEISHU_APP_ID` | 飞书自建应用 App ID |
| `FEISHU_APP_SECRET` | 飞书自建应用 App Secret |
| `FEISHU_OAUTH_REDIRECT_URI` | `feishu-auth` 的 HTTPS 回调地址 |
| `FEISHU_OAUTH_SCOPE` | `offline_access` 加应用已发布的多维表格用户身份权限（按飞书控制台生成的 scope 填写） |
| `FEISHU_TOKEN_ENCRYPTION_KEY` | 独立随机长字符串，用于加密用户 Token |
| `FEISHU_PLATFORM_ADMIN_IDS` | 平台管理员飞书 open_id/user_id，多个用英文逗号分隔 |

两种方式都写入 `feishu-user-bindings`。手机号绑定只保存飞书用户标识、手机号哈希及末四位；OAuth 回调成功后，Token 只以 AES-GCM 密文保存在云端，不返回小程序。

权限管理中要同时区分两类身份：

- **手机号绑定**：使用应用身份读写，但所有写操作先在云端核验已绑定飞书用户是否属于项目负责人；不能只依赖前端按钮。
- **OAuth 绑定**：使用用户身份读取和写入，权限同时受飞书用户自身权限约束。

`FEISHU_OAUTH_SCOPE` 使用空格分隔多个 scope。例如控制台确认这些 scope 支持用户身份时，可配置为 `offline_access base:record:update drive:file:upload`；最终以飞书权限页生成的实际 scope 为准。修改权限后必须新建应用版本并发布，项目经理也需重新绑定以授予新 scope。

## 云函数部署

在微信开发者工具中依次右键以下云函数，选择「上传并部署：云端安装依赖」：

1. `cloudfunctions/feishu-auth`
2. `cloudfunctions/feishu-rectification`
3. `cloudfunctions/quality-ledger`

然后打开该云函数的环境变量，填写：

| Key | 值 |
| --- | --- |
| `FEISHU_APP_ID` | 飞书自建应用 App ID |
| `FEISHU_APP_SECRET` | 飞书自建应用 App Secret |
| `FEISHU_BITABLE_APP_TOKEN` | `UHjvbqHtrak96Usb1ItcwI8pnNe` |
| `FEISHU_TABLE_ID` | `tbl8MwNtgzsHjG0A` |
| `FEISHU_FIELD_PROJECT_MANAGER` | `项目负责人` |

`feishu-rectification` 继续使用上表变量，并追加 `FEISHU_PLATFORM_ADMIN_IDS` 和与 `feishu-auth` **完全相同**的 `FEISHU_TOKEN_ENCRYPTION_KEY`。`quality-ledger` 也必须配置同一组 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_BITABLE_APP_TOKEN`、`FEISHU_TABLE_ID`、`FEISHU_FIELD_PROJECT_MANAGER` 和 `FEISHU_PLATFORM_ADMIN_IDS`，用于对每次云端写操作进行独立权限核验。管理员列表每次都从云函数环境变量实时判定，新增或移除管理员无需修改数据库绑定记录。

整改图片上传和“闭环”列更新会根据绑定方式选择令牌：OAuth 绑定使用 `user_access_token`；手机号绑定使用应用身份，但写入前必须通过项目负责人/平台管理员服务端核验。任一模式核验失败都不会写入。

不要把 `App Secret` 写进小程序源码、微信前端配置或聊天记录。

飞书应用需通过多维表格右上角菜单的“添加文档应用”加入该表，用于读取全部项目。项目经理本人则按飞书多维表格高级权限获得自己所负责项目的编辑权。应用至少开通并发布以下权限之一组：

- 多维表格完整读写 `bitable:app`；或
- 记录读取/更新权限（至少 `base:record:update`）以及文件上传权限 `drive:file:upload`。

仅把应用加入普通协作者、仅开只读权限，或者勾选权限后没有发布新的飞书应用版本，都会导致整改图片上传时报 `forbidden (HTTP 403 / 1061004)`。

## 字段映射

云函数会先读取新表字段结构，再自动识别常见列名。例如 `项目名称/项目归属`、`炉号/机组`、`存在质量问题/缺陷描述`、`问题/问题照片`、`闭环/闭环照片/整改后照片` 均可兼容。

本次联动不要求新增项目编码，也不会修改现有表格模板。最小联动必须存在以下字段：

- 项目选择：`项目名称`
- 编辑权限：`项目负责人`（飞书人员字段，可多选）
- 炉号选择：`炉号`
- 整改内容：`存在质量问题`（也可用 `缺陷描述`）
- 整改前附件：`问题`
- 整改后附件：`闭环`
- 建议保留：`缺陷部位`、`缺陷类型`、`缺陷等级`、`要求闭环时间`、`整改状态`

项目和炉号选项都直接来自同一张飞书表。同步时使用飞书原始文本做精确比较，不使用项目编码、别名或模糊匹配；因此名称相近的两个项目不会被合并，其他项目和其他炉号的缺陷也不会进入当前小程序项目。

如果需要把整改说明与提交时间也写回飞书，可在表格新增 `小程序整改说明`（多行文本）、`小程序提交时间`（日期时间）两列，并在云函数环境变量追加：

```text
FEISHU_FIELD_CLOSURE_NOTE=小程序整改说明
FEISHU_FIELD_CLOSURE_TIME=小程序提交时间
```

## 联调验收

1. 准备张经理和李经理两个飞书账号，并分别设置不同项目的 `项目负责人`。
2. 两个账号分别在小程序完成飞书授权绑定，确认都能查看全部项目。
3. 张经理进入自己的项目应显示“我负责”，进入李经理项目应显示“仅查看”。
4. 使用张经理账号尝试修改李经理项目，前端不显示编辑入口，直接调用云函数也必须返回 `PROJECT_READ_ONLY`。
5. 在飞书中创建一条整改记录，并填写 `项目名称`、`项目负责人`、`炉号`、整改内容及 `整改状态=待整改`。
6. 负责人在小程序同步并分享任务；整改人员通过分享链接上传说明和照片。
7. 负责人点击“通过并闭环”，检查飞书原记录的 `闭环` 字段是否出现整改照片。
8. 解除某用户飞书绑定或从 `项目负责人` 中移除后，确认该用户立即失去编辑权限。
# 常见部署问题

如果小程序提示“不支持的飞书同步操作：listProjectOptions”，说明小程序前端已经更新，但微信云端仍在运行旧版 `feishu-rectification`。

在微信开发者工具中右键 `cloudfunctions/feishu-rectification`，选择“上传并部署：云端安装依赖”，等待部署完成后重新编译小程序。只点击普通编译不会更新云函数。

如果错误包含 `-504003`、`FUNCTIONS_TIME_LIMIT_EXCEEDED` 或 `timed out after 3 seconds`，不是飞书权限错误，而是云函数仍使用默认 3 秒执行上限。上传部署完成后，请进入云开发控制台打开 `feishu-rectification` 的函数详情/高级配置，将“执行超时”改为 30 秒或 60 秒并保存，再回到小程序点击“重新读取”。
